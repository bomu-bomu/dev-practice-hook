- id: is-dev-branch-updated
  name: Is development branch updated
  description: Check if development branch is updated, dev should pull code and complete update their branch before merging or push code.
  entry: ./is_dev_branch_updated
  language: script
  pass_filenames: false
  always_run: true

- id: does-branch-last-too-long
  name: Does this branch last too long
  description: Check if this branch has been created longer than the threshold
  entry: ./does_branch_last_too_long
  language: script
  pass_filenames: false
  always_run: true
